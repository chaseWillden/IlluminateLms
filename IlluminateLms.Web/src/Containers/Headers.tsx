import * as React from 'react'
import {Typography} from 'material-ui'

export const H3 = (props: any) => <Typography variant="display2" gutterBottom>{props.children}</Typography>
export const H4 = (props: any) => <Typography variant="display1" gutterBottom>{props.children}</Typography>